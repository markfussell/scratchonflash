package com.chimu.scratchplayer.app {
    

/**
 * A ScratchApp is the outer running application
 * that provides services to the inner scratch
 * pieces.  Actually it currently only provides
 * logging, but that is useful enough to define
 * it.
 */
public interface ScratchApp {
    function logInfo(info : String) : void ;
}

}